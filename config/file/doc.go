// Package file provides a config.Source backed by files or directories.
//
// NewSource loads one file or every supported configuration file in a directory
// and infers the format from each file name.
package file
